# Configure Crowdsec Docker Notifications

Run the following command:
```bash
bash -c "$(wget -qLO - https://raw.githubusercontent.com/Reaper0x1/proxmox-scripts/main/crowdsec/configure-discord.sh)"
```